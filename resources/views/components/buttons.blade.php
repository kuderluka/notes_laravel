<input type="submit" text="Save {{$type}}" class="btn btn-primary">
<a href="{{ route($path) }}" class="btn btn-secondary">Go Back</a>
<a href="{{ route('index') }}" class="btn btn-secondary">Home</a>
